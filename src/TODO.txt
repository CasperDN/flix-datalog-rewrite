Tests:
1. Lattice tests. Test with GLB.
2. Test where the following happens:
    A non-full index is used on a delta relation.
    two facts are concluded in a single iteration of fixed point computation for that relation.
3. Test of provenance
